package main // Go files always start with package statement

// Import block, it allows importing multiple packages
import "fmt" // Automatically adds this line, if we forgot it

/*
The main function, when part of the main package,
identifies the entry point of an application
*/
func main() {
	fmt.Println("Hello, world!") // go run main.go
}
